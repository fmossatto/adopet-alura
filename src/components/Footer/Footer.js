import styles from './Footer.module.css';

const Footer = () => {
    return (
        <div className={styles.footer}>
            <p>2022 - Desenvolvido por Fmossatto</p>
        </div>
    );
};

export default Footer;
